// import { VerificationFlow } from "@/components/verification/VerificationFlow";
// import { ensureSession } from "@/lib/verification/session";

// export default async function VerifyPage() {
//   // Establish a session cookie on first load so /api/verify has something to check against.
//   await ensureSession();

//   return (
//     <main className="min-h-screen bg-slate-50">
//       <VerificationFlow />
//     </main>
//   );
// }
