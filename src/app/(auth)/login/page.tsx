import SignInComponent from "@/features/auth/components/login";

const SignInPage = async () => {
  // const user = await getCurrent();
  // if (user) {
  //   redirect("/workspaces");
  // }
  return <SignInComponent />;
};

export default SignInPage;
